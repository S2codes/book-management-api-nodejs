<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    <title>Update Book Details-Book Management</title>
</head>

<body>
    {{>navbar}}
    <main>
        <h2 class="text-center text-secondary my-4">Wellcome to Book Management System</h2>
        <div class="container my-5">
            <div class="row">
                <div class="col-md-10 col-12 mx-auto">
                    <h3>Update Book Details</h3>
                <p class="text-center text-danger m-0">{{errorMsg}}</p>
                    <form action="/update-book/{{book._id}}" method="post">
                        <label for="name">Book Name</label>
                        <input type="text" name="name" class="form-control mb-3" placeholder="Enter Book Name"
                            value="{{book.name}}" required>

                        <label for="name">Image</label>
                        <input type="text" name="image" class="form-control mb-3" placeholder="Enter Book Image"
                            value={{book.image}} required>

                        <div class="row">
                            <div class="col-md-4 col-12">
                                <label for="name">Author</label>
                                <input type="text" name="author" class="form-control mb-3"
                                    placeholder="Enter Author Name" value={{book.author}} required>
                            </div>
                            <div class="col-md-4 col-12">
                                <label for="name">Total Pages</label>
                                <input type="text" name="pages" class="form-control mb-3"
                                    placeholder="Total Pages of Book" value={{book.pages}} required>
                            </div>
                            <div class="col-md-4 col-12">
                                <label for="name">Book Price</label>
                                <input type="text" name="price" class="form-control mb-3" placeholder="Enter Book Price"
                                    value={{book.price}} required>
                            </div>

                            <button class="btn btn-primary my-3 w-50 mx-auto">Update Book</button>
                        </div>

                    </form>

                </div>
            </div>
        </div>
    </main>

    {{>footer}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>