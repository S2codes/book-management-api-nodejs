<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    <title>Book Management</title>
</head>

<body>
    {{>navbar}}
    <main>
        <h2 class="text-center text-secondary my-4">Wellcome to Book Management System</h2>
        <div class="container">
            <div class="row">
                <div class="col-md-10 col-12 mx-auto">
                    <h3>Add New Book</h3>
                    <a href="/add-book" class="btn btn-primary">Add New Book</a>
                    <div class="contaier mt-4 mb-5">
                        <h3>All Books</h3>
                        <div class="row">

                            {{#each books}}
                            <div class="col-md-3 col-12">
                                <div class="card" style="width: 100%;">
                                    <img src="{{this.image}}" class="card-img-top" height="150" alt="...">
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.name}}</h5>
                                        <span class="card-text text-secondary">{{this.author}}</span>
                                        <p class="card-text m-0">{{this.pages}} Pages</p>
                                        <p class="card-text m-0">{{this.price}} ₹</p>
                                        <div class="d-flex justify-content-between">
                                            <a href="/update-book/{{this._id}}" class="btn btn-primary my-2">Edit <i
                                                    class="bi bi-pencil-square"></i></a>
                                            <a href="/delete-book/{{this._id}}" class="btn btn-danger my-2">Delete <i
                                                    class="bi bi-trash"></i></a>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            {{/each}}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    {{>footer}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>